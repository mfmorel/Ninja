<Window x:Class="Ninja.View.NinjasListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ninja.View"
        mc:Ignorable="d"
        Title="NinjasListView" Height="300" Width="400"
        DataContext="{Binding Ninjas, Source={StaticResource Locator}}">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="35"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="35"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="35"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Grid.Column="2" FontSize="20" FontWeight="Bold">+</Button>
            <Label Grid.Row="0" Grid.Column="1" Height="50" FontWeight="Bold" FontSize="20" HorizontalAlignment="Center" VerticalContentAlignment="Center" VerticalAlignment="Center">Selecteer een ninja</Label>
            <StackPanel Grid.Row="1" Grid.ColumnSpan="3" Grid.Column="0">
                <ItemsControl ItemsSource="{Binding NinjaGrids}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate/>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
